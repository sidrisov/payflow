query GetFanTokenHolders($fanTokenName: String) {
    subjectTokens(where: {name: $fanTokenName}) {
        portfolio {
            balance
            user {
                id
            }
        }
    }
}